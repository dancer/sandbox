{
  "name": "sandbox",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --cwd apps/site dev",
    "build": "bun run build:packages && bun run --cwd apps/site build",
    "build:packages": "bun run --cwd packages/core build && bun run --cwd packages/local build && bun run --cwd packages/ai build",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "typecheck": "bun run build:packages && bun run --cwd apps/site typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "oxfmt": "^0.49.0",
    "oxlint": "^1.64.0",
    "typescript": "^5.9.3",
    "ultracite": "7.7.0"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide",
    "sharp",
    "unrs-resolver"
  ]
}
